<div class="page-name">
    <h3><%= @category.name %></h3>
</div>
<div class="category-show">
    <% if admin_signed_in? %>
        <%= link_to '<i class="fa fa-trash"></i>'.html_safe, category_path(@category), method: :delete, data: {confirm: "Estas seguro?"} %>
    <% end %>
    <div class="clothes">
        <% @category.cloths.each do |cloth| %>
            <div class="descr">
                <p><%= cloth.description %></p>
            </div>
        <% end %>
    </div>
</div>
